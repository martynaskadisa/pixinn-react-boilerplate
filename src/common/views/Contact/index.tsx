import * as React from 'react';

interface IProps { }

interface IState { }

class Contact extends React.Component<IProps, IState> {
  render () {
    return (
      <div>
        Contact
      </div>
    );
  }
}

export default Contact;
