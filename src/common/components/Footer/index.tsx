import * as React from 'react';

interface IProps { }

const Footer: React.StatelessComponent<IProps> = (props) => {
  return (
    <footer>
      <hr/>
      <p>Built by Pixinn</p>
    </footer>
  );
};

export default Footer;
